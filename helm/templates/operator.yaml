apiVersion: apps/v1
kind: Deployment
metadata:
  name: pystol-ui
spec:
  selector:
    matchLabels:
      name: pystol-ui
      component: pystol-ui
  replicas: 1
  template:
    metadata:
      labels:
        name: pystol-ui
        component: pystol-ui
    spec:
      containers:
        - name: pystol-ui
          image: {{ .Values.appSettings.pystol.ui.image }}
          ports:
            - containerPort: {{ .Values.appSettings.pystol.ui.port }}
          command: ["npm"]
          args: ["run", "server"]
          imagePullPolicy: Always
          env:
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
      serviceAccountName: pystol-acc

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pystol-controller
spec:
  selector:
    matchLabels:
      name: pystol-controller
      component: pystol-controller
  replicas: 1
  template:
    metadata:
      labels:
        name: pystol-controller
        component: pystol-controller
    spec:
      containers:
        - name: pystol-controller
          image: {{ .Values.appSettings.pystol.controller.image }}
          imagePullPolicy: Always
          command: ["pystol"]
          env:
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
      serviceAccountName: pystol-acc

